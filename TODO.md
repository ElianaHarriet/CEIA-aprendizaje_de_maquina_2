# TODO

## Pendiente

- [ ] Implementar endpoint `POST /predict` en `dockerfiles/fastapi/app.py`: cargar el modelo registrado desde MLflow y devolver la probabilidad de que un usuario califique una película con 4 o más estrellas a partir de un payload con las features del par (usuario, película).
